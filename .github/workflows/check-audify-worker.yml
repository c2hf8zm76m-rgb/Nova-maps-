name: Check Audify Worker

on:
  push:
    branches: [main]
    paths:
      - '_worker.js'
      - 'audify/**'
      - '.github/workflows/check-audify-worker.yml'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for Cloudflare deploy
        run: sleep 20
      - name: Check Audify search endpoint
        run: |
          set -x
          curl -i --fail --max-time 20 'https://weathered-truth-74c7.alo12230.workers.dev/api/audify-search?q=niska'
